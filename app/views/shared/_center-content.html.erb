<div class="post-view-title">
  <% score = current_end_user.quiz_score %>
  <% if score == 0 or score == 1 or score == 2 or score == 3 %>
    <p>LowRoom</p>
  <% elsif score == 4 or score == 5 or score == 6 %>
    <p>MiddleRoom</p>
  <% else %>
    <p>UpperRoom</p>
  <% end %>
</div>

<div class="posting">
  <% if posts %>
    <% posts.each do |post| %>
      <div class="card">
        <div class="card-body">
          <div class="post_end_user flex">
            <% if post.end_user.image.attached? %>
              <div class="profile-image"><%= image_tag post.end_user.image %></div>
            <% else %>
              <div class="profile-image"><img src="/assets/no_image.jpg"></div>
            <% end %>
            <h2 class="post_end_user_name"><%= post.end_user.name %><br><p><%= post.sentence %></p></h2>
            <div class="dropdown posting-dropdown">
              <a href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="fas fa-ellipsis-h"></i>
              </a>
              <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                <a class="dropdown-item" href="/posts/<%= post.id %>/edit"><i class="fas fa-edit"></i>　編集</a>
                <a data-confirm="本当に削除しますか？この操作は取り消せません。" class="dropdown-item" href="/posts/<%= post.id %>" data-method="delete"><i class="fas fa-trash-alt"></i>　削除</a>
              </div>
            </div>
            <p class="posting-time"><%= time_ago_in_words(post.updated_at) %>前</p>
          </div>
        </div>
      </div>
    <% end %>
  <% end %>
</div>

<div class="post-form-place flex">
  <% if @end_user.image.attached? %>
    <div class="profile-image"><%= image_tag @end_user.image %></div>
  <% else %>
    <div class="profile-image"><img src="/assets/no_image.jpg"></div>
  <% end %>
  <div class="post-form">
    <%= form_with(model: post, local: true) do |f| %>
      <textarea placeholder="みんなで話そう" name="post[sentence]" id="post-sentence" wrap="soft"></textarea>
      <%= f.hidden_field :end_user_id, :value => current_end_user.id %>
      <%= f.button :type => "submit", class: "post-button" do %>
        <i class="far fa-paper-plane"></i>
      <% end %>
    <% end %>
  </div>
</div>



